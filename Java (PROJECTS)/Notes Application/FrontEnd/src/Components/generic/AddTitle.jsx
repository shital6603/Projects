import React from "react";

const AddTitle = ({title}) =>
{
    return (
        <h1 className="text-center text-2xl text-blue-400 dark:text-purple-500 font-semibold">{title}</h1>
    );
}

export default AddTitle;